# Use deterministic fixtures without calling an AI provider.
GENERATIVE_UI_USE_MOCKS=true

# Required only when GENERATIVE_UI_USE_MOCKS=false.
AZURE_RESOURCE_NAME=
AZURE_API_KEY=
AZURE_MODEL=gpt-4o-mini